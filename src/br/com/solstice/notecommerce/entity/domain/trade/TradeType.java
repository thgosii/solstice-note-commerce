package br.com.solstice.notecommerce.entity.domain.trade;

public enum TradeType {

	EXCHANGE,	// Customer wants a new product (because they received a broken one)
	DEVOLUTION  // Customer wants to give back the product (in exchange of shop balance)
	
}
